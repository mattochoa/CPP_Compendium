---
type: system
tags: [system/directives]
pin: []
focus: []
hold: []
deepen: []
updated: 2026-09-23
---
# Directives: Editor → Builder

> [!essence] The Editor's standing instructions to the hourly Builder. The frontmatter steers the work queue (`pin` ids first, `focus` domains favoured, `hold` ids skipped, `deepen` ids used on DEEPEN runs). The sections below steer *how* to write. The Builder reads this every run; the Editor rewrites it every morning.

## Active Directives
1. **Phase 1 · Frame.** Write all 17 Domain Maps first (wave 0). Each must derive its domain's question from first principles and draw its concept map with *registered* titles, so the Atlas graph takes shape.
2. Treat the exemplars as the floor, not the ceiling: [[Value Categories]], [[Virtual Dispatch — vptr and vtable]], [[RAII]], [[Dangling Pointers and References]], [[Pointers vs References]], [[Map — Objects, Memory & Lifetime]].
3. Every note names at least one of the Five Tensions (tag `tension/...` plus a `[!tension]` or `[!principle]` callout).
4. Cite books with section **and** printed page (`Primer §13.6.2 (p. 532)`); verify C++14+ behavior against cppreference, because the Primer is C++11.
5. After wave 0, begin the Source Guides (`guide-primer`, `guide-tour`, `guide-web-references`) early in wave 1. They make every later citation faster.

## Quality Watchlist
- *(empty: the Editor adds recurring defects here with an example link)*

## Directive log
- 2026-09-23: initial directives (setup).
